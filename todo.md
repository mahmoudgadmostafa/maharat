# تعديلات مشروع محارات - حل مشكلة عرض ملفات PDF

## المطلوب:
- حل مشكلة عرض ملفات PDF في النافذة المنبثقة بحيث تظهر مباشرة داخل النافذة بدلاً من رسالة "لا يوجد صلاحيات" أو الفتح في نافذة جديدة.

## التحليل الحالي:
- الملف المسؤول عن عرض الموارد هو `src/components/common/ResourceModal.jsx`.
- روابط ملفات الـ PDF مخزنة في قاعدة بيانات Firebase ويتم إدارتها من خلال صفحة إدارة المحتوى في لوحة تحكم المعلم.
- المشكلة تكمن في أن `docs.google.com/gview` يتطلب أن يكون ملف PDF متاحًا للعامة أو أن يكون الرابط صحيحًا تمامًا للعرض المضمن.

## التعديلات المنفذة:
1. **تثبيت مكتبة react-pdf**: تم تثبيت مكتبة `react-pdf` لعرض ملفات PDF مباشرة داخل التطبيق.
2. **تحسين معالجة روابط Google Drive**: تم إضافة دالة `processGoogleDriveUrl` لاستخراج معرف الملف وتحويله إلى رابط `/preview` للعرض المباشر.
3. **إضافة نظام Fallback**: في حالة فشل عرض PDF باستخدام `react-pdf`, يتم التبديل تلقائيًا إلى استخدام `Google Docs Viewer` عبر iframe.
4. **تحسين واجهة المستخدم**: إضافة رسائل تحميل وتحسين أزرار التنقل بين الصفحات.
5. **تحسين الاستجابة**: جعل عرض PDF متجاوبًا مع أحجام الشاشات المختلفة.

## الخطوات التالية:
- ✅ تنفيذ التعديلات اللازمة لعرض PDF داخل النافذة المنبثقة.
- [ ] اختبار عرض ملفات PDF في النافذة المنبثقة.
- [ ] تسليم المشروع المعدل للمستخدم.

